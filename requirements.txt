# CLIF Code Status Analysis - Python Dependencies
# Install with: pip install -r requirements.txt

# Core data manipulation
pandas>=2.0.0
numpy>=1.24.0

# Configuration
pyyaml>=6.0

# Statistical analysis (required for regression with clustered SEs)
statsmodels>=0.14.0

# Optional but recommended
pyarrow>=12.0.0  # For faster parquet reading
openpyxl>=3.1.0  # If reading Excel files for cancer code lists

# Development/testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
